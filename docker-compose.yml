version: '2'
services:
   maven:
      build: boxfuse-sample-java-war-hello-custom
      environment:
        - TYPO3_CONTEXT=Development/Docker
   tomcat:
      build: tomcat
      links:
         - maven
      ports:
         - "8080:8080"
      volumes_from:
         - maven